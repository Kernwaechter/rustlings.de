fn main() {
    let number = "T-H-R-E-E"; // Diese Zeile nicht ändern
    println!("Buchstabiere eine Zahl: {number}");

    // TODO: Behebe den Compiler-Fehler, indem du die Zeile unten änderst,
    // ohne die Variable umzubenennen.
    number = 3;
    println!("Zahl plus zwei ist: {}", number + 2);
}
