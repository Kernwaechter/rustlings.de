// Boolesche Werte (`bool`)

fn main() {
    let is_morning = true;
    if is_morning {
        println!("Guten Morgen!");
    }

    // TODO: Definiere vor der `if`-Anweisung unten eine boolesche Variable mit dem Namen `is_evening`.
    // Der Wert der Variable soll die Verneinung (das Gegenteil) von `is_morning` sein.
    // let …
    if is_evening {
        println!("Guten Abend!");
    }
}
